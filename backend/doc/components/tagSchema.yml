components:
  schemas:
    tag:
      type: object
      properties:
        id:
          type: integer
          example: 1
        name:
          type: string
          example: "タグの名前"
        priority:
          type: integer
          example: 1
          